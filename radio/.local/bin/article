#! /usr/bin/python3

import article_parser
import sys
import re

try:
    title, content = article_parser.parse(url=sys.argv[1])
    print(f'# {title}')
    print(content)
except IndexError:
    raise Exception('Provide a url')
